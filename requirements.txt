# ============================================================
# 크레인 충돌 예측 시스템 - 필요 패키지 목록
# ============================================================
# 설치 방법: pip install -r requirements.txt
# ============================================================

# --- 웹 서버 ---
fastapi==0.115.0          # 웹 API 프레임워크 (백엔드 서버)
uvicorn[standard]==0.30.0 # ASGI 서버 (FastAPI 실행기)
jinja2==3.1.4             # HTML 템플릿 엔진
python-multipart==0.0.9   # 파일 업로드 처리

# --- 실시간 통신 ---
websockets==12.0          # WebSocket 통신 (실시간 데이터 전송)

# --- 수학/과학 계산 ---
numpy==1.26.4             # 수치 계산 (좌표 계산, 벡터 연산)

# --- 데이터 처리 ---
pydantic==2.9.0           # 데이터 검증 및 모델 정의

# --- 테스트 ---
pytest==8.3.0             # 테스트 프레임워크
pytest-asyncio==0.24.0    # 비동기 테스트 지원
httpx==0.27.0             # HTTP 클라이언트 (API 테스트용)
